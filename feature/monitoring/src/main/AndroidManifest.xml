<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!--
          Fall detection needs continuous accelerometer access, which Android
          only grants to a foreground service. FOREGROUND_SERVICE_TYPE_HEALTH is
          the honest declaration of what it is doing.
        -->
        <service
            android:name=".service.MonitoringService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!--
          The fall countdown is its own activity so it can appear over the lock
          screen. A patient on the floor cannot unlock the phone and navigate to
          a cancel button.
        -->
        <activity
            android:name=".service.FallConfirmationActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:showOnLockScreen="true"
            android:turnScreenOn="true" />

        <receiver
            android:name=".service.BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
